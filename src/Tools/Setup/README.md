# Setup tools

Tools that prepare this machine for the other tools.

Rules:

- They may log in to test a connection, but never modify a galaxy.
- Galaxy logins belong in `config\credentials.local.ini`, which git ignores, never in committed files.
- Name tools `Save...` or `Configure...`.

No tools yet.
